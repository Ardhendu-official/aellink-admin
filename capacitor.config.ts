import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.puspendu.recipe',
  appName: 'Recipe',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
